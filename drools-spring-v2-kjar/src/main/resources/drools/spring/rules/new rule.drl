package drools.spring.rules;

import drools.spring.example.model.Symptom;

rule "new rule"
    no-loop
    when
        $s: Symptom( )
    then
        System.out.println($s);
        $s.setDescription("Nesto Noviji opis");
        update($s);
end
